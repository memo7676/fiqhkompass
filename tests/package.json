{
  "name": "fiqh-kompass-rules-tests",
  "private": true,
  "scripts": {
    "test": "cd .. && firebase emulators:exec --only firestore --project fiqh-test \"node tests/firestore.rules.test.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.14.1",
    "firebase-tools": "^15.0.0"
  }
}
